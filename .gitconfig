[user]
	name = Cédric Messiant
	email = cedricmessiant@ecreall.com

[push]
    default = simple

[core]
    excludesfile = /home/cedricmessiant/.gitignore

[color]
    ui = auto

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = blue bold
    frag = magenta bold
    old = red bold
    new = green bold
    whitespace = red reverse

[color "status"]
    added = green
    changed = yellow
    untracked = cyan

[alias]
    st = status
    ci = commit
    amend = commit --amend
    br = branch
    co = checkout
    df = diff
    dc = diff --cached
    pr = pull --rebase

### from http://fle.github.io/git-tip-a-simple-gitconfig-file.html
# st = status
# ci = commit
# br = branch
# co = checkout
# df = diff
# dc = diff --cached
# lg = log -p
# pr = pull --rebase
# gr = log --all --graph --decorate --oneline
### from http://pioupioum.fr/developpement/git-alias-productivite.html
# amend = commit --amend
# st = status
# ci = commit
# co = checkout
# who = shortlog -sne
# oneline = log --pretty=oneline --abbrev-commit --graph
# changes = diff --name-status
# dic = diff --cached
# diffstat = diff --stat
# lc = !git oneline ORIG_HEAD.. --stat --no-merges
# addm = !git ls-files -m -z | xargs -0 git-add && git status
# addu = !git ls-files -o --exclude-standard -z | xargs -0 git-add && git status
# rmm = !git ls-files -d -z | xargs -0 git-rm && git status
[http]
	sslVerify = false
